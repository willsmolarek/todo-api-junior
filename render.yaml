services:
  - type: web
    name: todo-api-junior
    runtime: node
    buildCommand: echo "Using JavaScript version for deployment"
    startCommand: node dist/server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health
