services:
  - type: web
    name: todo-api-junior
    runtime: node
    buildCommand: echo "Using simple JavaScript server"
    startCommand: node server.js
    envVars:
      - key: NODE_ENV
        value: production
    healthCheckPath: /health